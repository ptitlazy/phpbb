services:
    vendor_notifications.controller:
        class: vendor\notifications\controller\controller
        arguments:
            - @controller.helper
            - @path_helper
            - @template
            - @config
            - @user
            - @notification_manager
            - %core.root_path%
            - %core.php_ext%

    notification.type.test:
        class:  vendor\notifications\notification\type\test
        scope: prototype # scope MUST be prototype for this to work!
        arguments:
            - @user_loader
            - @dbal.conn
            - @cache.driver
            - @user
            - @auth
            - @config
            - %core.root_path%
            - %core.php_ext%
            - %tables.notification_types%
            - %tables.notifications%
            - %tables.user_notifications%
        tags:
            - { name: notification.type }
